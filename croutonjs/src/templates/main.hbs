<div id="bmlt-tabs-table">
    <div id="bmlt-header" class="bmlt-header hide">
        {{> header}}
    </div>
<!--	{{#each this.meetings.buttonFilters}}-->
<!--		<div id="byfield_{{ @key }}" class="bmlt-page hide">-->
<!--			{{> byfields this }}-->
<!--		</div>-->
<!--	{{/each}}-->
    <div id="byday" class="bmlt-page">
		<ul class="nav nav-tabs">
			{{#each this.config.day_sequence}}
				<li><a href="#tab{{this}}">{{getDayOfTheWeek this}}</a></li>
			{{/each}}
		</ul>
        {{> bydays this.meetings.bydays}}
    </div>
    <div id="tabs-content" class="bmlt-page hide">
        {{> weekdays this.meetings.weekdays}}
    </div>
</div>
